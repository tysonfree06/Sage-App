class AppDurations {
  static const Duration extraSmall = Duration(milliseconds: 500);

  static const Duration keyboardOpenDuration = Duration(milliseconds: 600);

  static const Duration smallSmall = Duration(seconds: 1);

  static const Duration small = Duration(seconds: 2);

  static const Duration medium = Duration(seconds: 4);

  static const Duration snackbarErrorDuration = Duration(seconds: 7);

  static const Duration large = Duration(seconds: 8);

  static const Duration timeoutDuration = Duration(seconds: 15);
}
